/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or any plugin's vendor/assets/stylesheets directory can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any styles
 * defined in the other CSS/SCSS files in this directory. It is generally better to create a new
 * file per style scope.
 *
 */
@charset "utf-8";

// Bootstrapテーマのインポート
@import "bootstrap-sprockets";
@import "bootswatch/cosmo/variables";
@import "bootstrap";
@import "bootswatch/cosmo/bootswatch";

/*-----------------------------

　リセットCSS + サイト共通設定

------------------------------*/

/* 全ての要素に対する余白初期値設定 */
html, body, div, span, applet, object, iframe,
h1, h2, h3, h4, h5, h6, p, blockquote, pre,
a, abbr, acronym, address, big, cite, code,
del, dfn, em, img, ins, kbd, q, s, samp,
small, strike, strong, sub, sup, tt, var,
b, u, i, center,
dl, dt, dd, ol, ul, li,
fieldset, form, label, legend,
table, caption, tbody, tfoot, thead, tr, th, td,
article, aside, canvas, details, embed, 
figure, figcaption, footer, header, main, 
menu, nav, output, ruby, section, summary,
time, mark, audio, video{
	margin:0;
	padding:0;
	border: 0;
	font-size: 100%;
}

/* デフォルト文字設定 */
body{
	/*color:#666666;*/
  color:#fff;
	font-family: "HG丸ゴシックM-PRO", "HGMaruGothicMPRO";
	line-height:1.5;
}

/* リストマーカーを非表示 */
ul,ol{
	list-style-type:none;
}

/* テーブルセルのボーダーの設定 */ 
table {
	border-collapse: collapse; 
	border-spacing: 0;
}

/* 画像とフォームパーツの上下位置をテキストの真ん中に */
img, input, select, textarea { 
	vertical-align: middle;
}

/* 画面タイトルのフォントを指定 */
h1{
	font-family: "HG丸ゴシックM-PRO", "HGMaruGothicMPRO";
}
/* デフォルトリンク色の設定 */
a{
  text-decoration: none;
}

a:hover{
  background: none;
	opacity:0.7;
}

/* 画像リンクの設定 */
a img{
	opacity:1;
	filter: alpha(opacity=100);
}

a:hover img{
	opacity:0.7;
	filter: alpha(opacity=75);
}

/* 角丸 */ 
.radius{
	border-radius: 20px;
  color: #fff;
}

.radius:hover{
	opacity:0.7;
  cursor: pointer;
}

/*	clearfix
------------------------------*/
.clearfix:after {/*for modern browser*/
   content: "";
   display: block;
   clear: both;
}

.clearfix { *zoom:1; } /*for IE6,IE7*/


/*--------------------------

　レイアウト

---------------------------*/
.inner{
  margin: 0 auto;
}


/* ヘッダー
---------------------------*/
#header{
  height: 45px;
  background:-webkit-linear-gradient(left, #f90, #fc6); /*webkit*/
  background:linear-gradient(left, #f90, #fc6); /*IE10 & future */
	font-size: 200%;
}

/* フッター
---------------------------*/
#footer{
  clear: both;
  height: 35px;
  background:-webkit-linear-gradient(left, #f90, #fc6); /*webkit*/
  background:linear-gradient(left, #f90, #fc6); /*IE10 & future */
}

/* グローバルナビゲーション
---------------------------*/
#gnav{
  background: #00c4ab
}

#gnav a{
  color: #fff;
}

/* コンテンツエリア
---------------------------*/
#contents{
  width: 960px;
  padding: 0 10px 0 0;
}

/* メインコンテンツ
---------------------------*/
#main{
  width: 790px;
  height: 580px;
  padding: 5px 10px;
  margin: 0 auto;
  overflow: auto;
  float: right;
}

/* サイドバー
---------------------------*/
#side{
  width: 160px;
  height: 580px;
  padding: 15px 10px;
  background-color: #d7d0c3;
  text-align: center;
  font-size: 20px;
  position: relative;
  float:left;
}

#side a:hover{
}

#normal{
}

.normal_btn{
  width: 120px;
  height: 80px;
  margin: 15px 0;
  border: none;
  background: #f90;
  font-size: 20px;
	font-family: "HG丸ゴシックM-PRO", "HGMaruGothicMPRO";
}

.normal_link{
  width: 120px;
  height: 80px;
  background: #f90;
  margin: 15px auto;
  display: table-cell;
  vertical-align: middle;
	font-family: "HG丸ゴシックM-PRO", "HGMaruGothicMPRO";
}

#admin{
  position: absolute;
  bottom: 0;
  display: table;
  border-collapse: separate;
  border-spacing: 0 15px;
  margin: 0 10px;
}

.admin_link{
  width: 120px;
  height: 70px;
  background: #658aff;
  margin: 15px 0;
  display: table-cell;
  vertical-align: middle;
	font-family: "HG丸ゴシックM-PRO", "HGMaruGothicMPRO";
}

/*--------------------------

　共有スタイル

---------------------------*/
.black_font{
  color: #000;
}


/* カスタム ラジオボタン
---------------------------*/
input[type=radio].custom_rd{
  display: none;
}

label.custom_rd{
  padding: 10px 10px;
  margin: 10px 20px;
  background: #969696;
}

// 選択したらオレンジ色
input[type=radio]:checked + .orange_rd{
  background: #f90;
}

// 選択したら緑色
input[type=radio]:checked + .green_rd{
  background: #91ff37;
}

// 選択したら青色
input[type=radio]:checked + .blue_rd{
  background: #658aff;
}

@import "login";
@import "menu";
@import "leafs";
@import "contracts";
@import "daily_contracts_report";
@import "count_contracts_summary";
@import "multi_seals_update";
